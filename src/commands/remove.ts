
import { resolvePackages } from '../utils'
import { createMultiProgressBar } from '../render';
import type { SingleBar } from 'cli-progress'
import c from 'picocolors'

export async function resolveRemove() {
  // const pkgMeta = await resolvePackages()
  // const bars = createMultiProgressBar()
  // let packagesBar: SingleBar | undefined
  // const depBar = bars?.create(100, 0, { type: c.cyan('loading') })

}
